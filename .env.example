# Ably API key for the server-side token endpoint.
# Get one from https://ably.com/accounts → your app → API Keys.
# This must NOT be exposed to the browser — it lives only in the serverless fn.
ABLY_API_KEY=your-app-id.your-key-id:your-key-secret
